; Clojure comes with many predicates that is used to test a condition
;
; on objects:
; 	class? coll? decimal? float? fn? instance? integer? isa? keyword?
; 	list? macro? map? number? seq? set? string? vector?
;
; 	some extras: ancestors, bases, class, ns-publics and parents
;
; relationships:
; 	< <= = not= == > >= compare distinct? identical?
;
; 	also: and or not true? false? nil?
;
; sequences:
;	empty? not-empty? every? not-every? some not-any?